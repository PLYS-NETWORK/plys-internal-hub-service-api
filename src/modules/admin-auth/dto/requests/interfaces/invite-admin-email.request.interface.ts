/**
 * camelCase TS-internal shape of the admin allow-list invite body.
 * Single field; the JSON contract uses the same `email` key.
 */
export interface IInviteAdminEmailRequest {
  readonly email: string;
}
