{
  "name": "platform-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/platform-service/src",
  "projectType": "application",
  "tags": ["scope:app", "type:service", "domain:platform"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build && rm -rf dist/i18n && cp -r ../../packages/common-nest/i18n dist/i18n",
        "cwd": "apps/platform-service"
      },
      "dependsOn": ["^build"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "NODE_ENV=development nest start --watch",
        "cwd": "apps/platform-service"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "NODE_OPTIONS=--max-old-space-size=8192 eslint \"src/**/*.ts\" --max-warnings=0",
        "cwd": "apps/platform-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config jest.config.js",
        "cwd": "apps/platform-service"
      }
    }
  }
}
