export enum ProjectPaymentType {
  PER_TASK = 'per_task',
  PER_MONTH = 'per_month',
}

export const PROJECT_PAYMENT_TYPES: readonly ProjectPaymentType[] = [
  ProjectPaymentType.PER_TASK,
  ProjectPaymentType.PER_MONTH,
];
